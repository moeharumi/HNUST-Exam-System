#include <stdio.h>
void  main( )
{
int flag, i, n;
int repeat, ri;
double item, sum;
scanf("%d", &repeat);
for(ri = 1; ri <= repeat; ri++)
{
scanf("%d", &n);
/**********Program**********/
//只能用上述已定义的变量，如循环变量用变量i，正负符号用变量flag，
//求和项用变量item等等
//循环用for
/**********  End  **********/
printf("sum = %.3f\n", sum);
}
}