/*------------------------------------------------------    
【程序改错】
--------------------------------------------------------
功能：有1、2、3、4个数字，能组成多少个互不相同且无重复数
      字的三位数？都是多少？
------------------------------------------------------*/
#include <stdio.h>
main()
{
  int i,j,k;
  /**********FOUND**********/
  printf("\n")
  /**********FOUND**********/
  for(i=1;i<=5;i++)             /*只修改错误的地方其他不要修改否则不得分*/
    for(j=1;j<5;j++)
      for (k=1;k<5;k++)
      {
        /**********FOUND**********/
        if (i!=k||i!=j||j!=k)
          printf("%d,%d,%d\n",i,j,k);
      }
}