#include <stdio.h>
output(long b,long i)
{
printf("\n%ld=800*%ld+9*%ld+1\n",b,i,i);
printf("??代表的两位数: %d",i);
printf("\n809*??后的结果：%d",b);
getch();
}
main()
{
/**********FOUND**********/
long int a;b;i;  a,b,i
a=809;
/**********FOUND**********/
for(i=10;i<100,i++)      ;
{
/**********FOUND**********/
b=i/a+1;
if(b>=1000&&b<=10000&&8*i<100&&9*i>=100)
output(b,i);
}
}